node_modules
dist
.wrangler
*.tsbuildinfo
.DS_Store
tmp

# Holocron writes its in-memory Vitest page cache through this fallback path.
website/dummy-non-existing-folder

# Secrets — this repo is public. Never commit any of these.
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
*.pem
*.key
